import tkinter as tk
from tkinter import *

class Login():
    def __init__(self, root):
        self.ventana = Toplevel(root)
        self.ventana.title("Iniciar Sección")

        self.ventana.mainloop()